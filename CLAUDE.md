Never modify src/lib/numerology.ts, any file in src/services/agents/, or the existing razorpay-payment function. All new features must be added as new files only.Always test that the existing free-check flow and baby-name generator still work after any change.
 
